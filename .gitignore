# Fuzz corpus (generated data, not source)
corpus/

# LibFuzzer crash artifacts
crash-*
leak-*
timeout-*
oom-*

# Build trees
build/
build_meson/

# Meson subproject clones (regenerated by meson setup)
subprojects/libev/
subprojects/AllocNBuffer/
subprojects/uthash/
subprojects/Unity-*/
subprojects/packagecache/
subprojects/.wraplock

# IDE / clangd
.cache/
**/.cache/
compile_commands.json

# Local certs / keys
*.pem
cert.pem
key.pem

# Editor / OS noise
*~
*.swp
.DS_Store

# Integration test build artifacts
tests/integration/neqo-test/wt-test-client/target/
tests/integration/neqo-test/wt-test-client/Cargo.lock
